#!/bin/bash

set -e # exit immediately if a simple command exits with a non-zero status
set -u # report the usage of uninitialized variables

# Setup env vars and folders for the webapp_ctl script
source /var/vcap/jobs/logstash-cf/helpers/ctl_setup.sh 'logstash-cf'

export PORT=${PORT:-5000}
export LANG=en_US.UTF-8
export FILTER_WORKERS=${FILTER_WORKERS:-1}

case $1 in

  start)
    pid_guard $PIDFILE $JOB_NAME

    export PATH=/var/vcap/packages/java/bin:$PATH
    export JAVA_HOME=/var/vcap/packages/java/

    exec /var/vcap/jobs/logstash-cf/packages/logstash/bin/logstash \
        agent -w $FILTER_WORKERS -f /var/vcap/jobs/logstash-cf/config/logstash.conf \
        >>  $LOG_DIR/logstash-cf.stdout.log \
        2>> $LOG_DIR/logstash-cf.stderr.log &

    echo $! >> $PIDFILE

    ;;

  stop)
    kill_and_wait $PIDFILE

    ;;
  *)
    echo "Usage: cf_ctl {start|stop}"

    ;;

esac
exit 0
